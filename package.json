{
  "name": "mui-sonner",
  "private": true,
  "scripts": {
    "build": "turbo build --filter=mui-sonner --force",
    "n:dev": "turbo dev --filter=example-next",
    "v:dev": "turbo dev --filter=example-vite",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prettier": "^3.1.1",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@8.19.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
